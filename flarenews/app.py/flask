from flask import Flask, render_template, request
import joblib

app = Flask(__flarenews__)

# Load the trained model
model = joblib.load('path/to/your/flarenewsc')

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/predict', methods=['POST'])
def predict():
    if request.method == 'POST':
        news_text = request.form['news_text']
        # Vectorize the input text
        # Add code here for vectorization based on your model requirements
        # Example: news_tfidf = vectorizer.transform([news_text])
        # Make prediction
        # Example: prediction = model.predict(news_tfidf)[0]
        # Result (modify as per your model output): result = "Fake" if prediction == 1 else "Real"
        result = "Fake"  # Placeholder, replace with actual result
        return render_template('result.html', result=result, news_text=news_text)

if __name__ == '__main__':
    app.run(debug=True)
