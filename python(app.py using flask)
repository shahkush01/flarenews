from flask import Flask, render_template, request

app = Flask(__name__)

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/check', methods=['POST'])
def check_news():
    news_text = request.form['news']
    # Implement your news detection logic here
    # You might want to use natural language processing or machine learning models for better accuracy
    # For simplicity, let's assume a basic check where any text is considered true
    result = "True" 

    return render_template('index.html', result=result)

if __name__ == '__main__':
    app.run(debug=True)
